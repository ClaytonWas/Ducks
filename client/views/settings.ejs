<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Account Settings - Ducks</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        'club-blue': '#87CEEB',
                        'club-teal': '#4ECDC4',
                        'club-dark': '#2C5F7C',
                    }
                }
            }
        }
    </script>
    <style>
        @keyframes float {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-10px); }
        }
        .float-animation {
            animation: float 3s ease-in-out infinite;
        }
    </style>
</head>
<body class="min-h-screen bg-gradient-to-br from-sky-300 via-sky-200 to-blue-300 flex items-center justify-center p-4 relative overflow-hidden">
    <!-- Decorative penguins -->
    <div class="absolute top-10 left-10 text-6xl opacity-10 float-animation">🐧</div>
    <div class="absolute bottom-10 right-10 text-6xl opacity-10 float-animation" style="animation-delay: 1s;">❄️</div>
    
    <div class="w-full max-w-md relative z-10">
        <p id="updateMessage" class="mb-4 p-4 rounded-2xl text-center font-semibold hidden shadow-lg"></p>
        <div class="bg-white/95 backdrop-blur-md rounded-3xl p-10 shadow-2xl border-4 border-sky-200 transform hover:scale-[1.02] transition-transform duration-300">
            <div class="text-center mb-6">
                <h1 class="text-5xl font-bold text-club-dark mb-2 float-animation">🐧</h1>
                <h2 class="text-3xl font-bold text-club-dark mb-2">Account Settings</h2>
                <p class="text-club-dark text-lg">Welcome, <strong class="text-club-teal"><%= username %>!</strong></p>
            </div>
            
            <form id="settingsForm">
                <div class="mb-5">
                    <label for="shape" class="block text-club-dark font-bold mb-2 text-lg">🎨 Your Shape</label>
                    <select
                        id="shape"
                        name="shape"
                        class="w-full px-5 py-4 rounded-2xl border-3 border-sky-200 focus:border-club-teal focus:outline-none focus:ring-4 focus:ring-club-teal/30 transition-all text-lg shadow-inner bg-white/80 hover:bg-white cursor-pointer"
                    >
                        <option value="cube" <%= shape === 'cube' ? 'selected' : '' %>>🔲 Cube - Classic and Square</option>
                        <option value="sphere" <%= shape === 'sphere' ? 'selected' : '' %>>⚪ Sphere - Round and Smooth</option>
                        <option value="cone" <%= shape === 'cone' ? 'selected' : '' %>>🔺 Cone - Pointy and Fun</option>
                    </select>
                </div>
                
                <div class="mb-6">
                    <label for="color" class="block text-club-dark font-bold mb-2 text-lg">🌈 Your Color</label>
                    <div class="flex items-center gap-5">
                        <input
                            type="color"
                            id="color"
                            name="color"
                            value="<%= color %>"
                            class="w-24 h-16 rounded-2xl border-4 border-sky-200 cursor-pointer hover:scale-110 transition-transform shadow-lg"
                        />
                        <div class="flex-1">
                            <div class="w-full h-16 rounded-2xl border-4 border-sky-200 shadow-lg flex items-center justify-center" id="colorPreview" style="background-color: <%= color %>">
                                <span class="text-white font-bold text-lg drop-shadow-lg">Your Color!</span>
                            </div>
                        </div>
                    </div>
                </div>
                
                <div class="flex gap-4 mb-6">
                    <button
                        type="button"
                        id="saveButton"
                        class="flex-1 bg-gradient-to-r from-club-teal via-teal-500 to-teal-600 text-white py-4 px-6 rounded-2xl font-bold text-lg hover:from-teal-600 hover:via-teal-500 hover:to-club-teal transform hover:-translate-y-2 hover:scale-105 transition-all shadow-2xl hover:shadow-teal-500/50"
                    >
                        💾 Save Changes
                    </button>
                    <button
                        type="button"
                        id="cancelButton"
                        class="flex-1 bg-gradient-to-r from-orange-400 via-orange-500 to-orange-600 text-white py-4 px-6 rounded-2xl font-bold text-lg hover:from-orange-600 hover:via-orange-500 hover:to-orange-400 transform hover:-translate-y-2 hover:scale-105 transition-all shadow-2xl hover:shadow-orange-500/50"
                    >
                        Cancel
                    </button>
                </div>
            </form>
            
            <div class="flex flex-col gap-3 pt-6 border-t-2 border-sky-200">
                <a href="/home" class="bg-gradient-to-r from-sky-200 to-sky-300 text-club-dark py-3 px-4 rounded-2xl font-semibold hover:from-sky-300 hover:to-sky-200 transform hover:-translate-y-1 transition-all shadow-md text-center border-2 border-sky-200">
                    🏠 Home
                </a>
                <a href="/join" class="bg-gradient-to-r from-sky-200 to-sky-300 text-club-dark py-3 px-4 rounded-2xl font-semibold hover:from-sky-300 hover:to-sky-200 transform hover:-translate-y-1 transition-all shadow-md text-center border-2 border-sky-200">
                    🎮 Play Game
                </a>
                <a href="/logout" class="bg-gradient-to-r from-sky-200 to-sky-300 text-club-dark py-3 px-4 rounded-2xl font-semibold hover:from-sky-300 hover:to-sky-200 transform hover:-translate-y-1 transition-all shadow-md text-center border-2 border-sky-200">
                    🚪 Logout
                </a>
            </div>
        </div>
    </div>
    
    <script src="/scripts/settingsHandler.js"></script>
    <script>
        // Update color preview
        document.addEventListener('DOMContentLoaded', () => {
            const colorInput = document.getElementById('color')
            const colorPreview = document.getElementById('colorPreview')
            
            if (colorInput && colorPreview) {
                colorInput.addEventListener('input', (e) => {
                    colorPreview.style.backgroundColor = e.target.value
                })
            }
        })
    </script>
</body>
</html>
